---
app:
  window: 3s                 # time window to calculate request rate (seconds)
  max_difficulty: 20         # max lower bits of challenge solution to be zero
  rate_difficulty_factor: 1

log:

net:
  host: 0.0.0.0
  port: 9090
  buff_size: 1024
  timeout: 1s
  max_connections: 10
